<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tnt.ethiopianmoviesboxoffice">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".EthiopianMoviesBoxOfficeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon_2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon_2_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EthiopianMoviesBoxOffice"
        android:networkSecurityConfig="@xml/network_security_config">


       <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.EthiopianMoviesBoxOffice">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>